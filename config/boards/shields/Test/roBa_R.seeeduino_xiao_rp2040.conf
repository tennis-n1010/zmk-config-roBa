# RP2040 にはBLEコントローラが無いので無効化
CONFIG_ZMK_BLE=n
CONFIG_BT=n
CONFIG_BT_HCI_DRIVER=n

# USBでテストする
CONFIG_USB_DEVICE_STACK=y
CONFIG_ZMK_USB=y

# 既存のPMW3610設定はそのままでOK（必要ならここにまとめても良い）
CONFIG_SPI=y
CONFIG_INPUT=y